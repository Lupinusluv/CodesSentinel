# Backend API base URL (no trailing slash)
# Change for Docker Compose or remote deployment
VITE_API_URL=http://localhost:8000

# WebSocket base URL — defaults to VITE_API_URL if unset
# Set explicitly if WS uses a different port/host
VITE_WS_URL=ws://localhost:8000
